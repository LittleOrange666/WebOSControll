services:
  webos-bot:
    image: littleorange666/webos_alarm:1.0.8
    restart: on-failure
    environment:
      - TV_MAC=${TV_MAC}
      - DC_TOKEN=${DC_TOKEN}
      - HA_TOKEN=${HA_TOKEN}
      - HA_HOST=${HA_HOST}
      - TV_ENTITY=${TV_ENTITY}
      - GC_ENTITY=${GC_ENTITY}
      - MEDIA_ID=${MEDIA_ID}
      - YT_TARGET=${YT_TARGET}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
    network_mode: "host"